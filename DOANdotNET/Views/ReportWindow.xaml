<Window x:Class="DOANdotNET.Views.ReportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Báo Cáo Doanh Thu"
        Height="650"
        Width="1100"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- TITLE -->
        <TextBlock Text="BÁO CÁO DOANH THU BÃI ĐỖ XE"
                   FontSize="28"
                   FontWeight="Bold"
                   Foreground="#2C3E50"
                   Margin="0,0,0,20"/>

        <!-- FILTER -->
        <Border Grid.Row="1"
                Background="White"
                CornerRadius="10"
                Padding="15"
                Margin="0,0,0,15">

            <StackPanel Orientation="Horizontal"
                        VerticalAlignment="Center">

                <TextBlock Text="Từ ngày:"
                           VerticalAlignment="Center"
                           Margin="0,0,10,0"/>

                <DatePicker x:Name="dpFrom"
                            Width="150"
                            Margin="0,0,20,0"/>

                <TextBlock Text="Đến ngày:"
                           VerticalAlignment="Center"
                           Margin="0,0,10,0"/>

                <DatePicker x:Name="dpTo"
                            Width="150"
                            Margin="0,0,20,0"/>

                <Button Content="Xem Báo Cáo"
                        Width="130"
                        Height="35"
                        Click="BtnLoadReport_Click"/>

            </StackPanel>
        </Border>

        <!-- TABLE -->
        <Border Grid.Row="2"
                Background="White"
                CornerRadius="10"
                Padding="10">

            <DataGrid x:Name="dgReport"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      FontSize="14">

                <DataGrid.Columns>

                    <DataGridTextColumn Header="Mã GD"
                                        Binding="{Binding MaGiaoDich}"
                                        Width="120"/>

                    <DataGridTextColumn Header="Khách Hàng"
                                        Binding="{Binding TenKhachHang}"
                                        Width="*"/>

                    <DataGridTextColumn Header="Biển Số"
                                        Binding="{Binding BienSoXe}"
                                        Width="150"/>

                    <DataGridTextColumn Header="Loại Xe"
                                        Binding="{Binding MaLoaiXe}"
                                        Width="120"/>

                    <DataGridTextColumn Header="Thời Gian Vào"
                                        Binding="{Binding ThoiGianVao}"
                                        Width="180"/>

                    <DataGridTextColumn Header="Thời Gian Ra"
                                        Binding="{Binding ThoiGianRa}"
                                        Width="180"/>

                    <DataGridTextColumn Header="Thành Tiền"
                                        Binding="{Binding ThanhTien, StringFormat=N0}"
                                        Width="150"/>

                </DataGrid.Columns>

            </DataGrid>

        </Border>

        <!-- TOTAL -->
        <Border Grid.Row="3"
                Background="White"
                CornerRadius="10"
                Padding="15"
                Margin="0,15,0,0">

            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Right">

                <TextBlock Text="Tổng doanh thu: "
                           FontSize="20"
                           FontWeight="Bold"/>

                <TextBlock x:Name="txtTotalRevenue"
                           FontSize="20"
                           FontWeight="Bold"
                           Foreground="Green"/>

            </StackPanel>

        </Border>

    </Grid>
</Window>